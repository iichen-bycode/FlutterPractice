import 'package:flutter/material.dart';

class ProgressSlider extends StatefulWidget {
  @override
  ProgressSliderState createState() => new ProgressSliderState();
}

class ProgressSliderState extends State<ProgressSlider> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ProgressSlider'),
      ),
    );
  }
  @override
  void initState() {
    super.initState();
  }

  @override
  void dispose() {
    super.dispose();
  }

}